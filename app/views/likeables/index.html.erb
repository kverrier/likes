<h3>Likeables</h3>
<ul class="likeables">
  <% @likeables.each do |likeable| %>
    <li>
      <%= link_to likeable.name, likeable %>
    </li>
  <% end %>
</ul>

Search for a new like:
<%= render "layouts/searchbar" %>
